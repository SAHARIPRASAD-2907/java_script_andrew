const isRaining=true
//cannot initialize new const variable
//isRaining=false
console.log(isRaining);

//this only works for variable value 
//object value can be changed if if 
//items inside value is changed

const person={
    age:27
}
person.age=28
//person={}

console.log(person)